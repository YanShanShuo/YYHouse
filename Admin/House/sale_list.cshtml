@{
    Page.Title = "出售列表";
    Layout = "_LayoutAdmin.cshtml";
}

<div style="text-align:center;font-size:20px;font-weight:bold">
    出售列表
</div>
<div class="am-g">
    <div class="am-u-sm-12">
        <form class="am-form">
            <table class="am-table am-table-striped am-table-hover table-main">
                <thead>
                    <tr>
                        <th class="table-check"><input type="checkbox" /></th>
                        <th class="table-id">ID</th>
                        <th class="table-title">标题</th>
                        <th class="table-date">区-小区</th>
                        <th class="table-date">所属经纪人</th>
                        <th class="table-date">精品</th>
                        <th class="table-date">价格</th>
                        <th class="table-set">操作</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        foreach (Model.Article a in list)
                        {

                            
                            <tr>
                                <td><input type="checkbox" /></td>
                                <td>@a.ID</td>
                                <td>@a.Title</td>
                                <td class="am-hide-sm-only">@a.UpdateTime</td>
                                <td>
                                    <div class="am-btn-toolbar">
                                        <div class="am-btn-group am-btn-group-xs">
                                            <button class="am-btn am-btn-default am-btn-xs am-text-secondary" data="@a.ID" name="edit" type="button"><span class="am-icon-pencil-square-o"></span>编辑</button>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </form>
    </div>
</div>